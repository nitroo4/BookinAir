namespace BOOKINGAPI.Enums;

// Enum pour les rôles
public enum UserRole
{
    Client,
    Admin,
    Services,
    Superviseur
}

// Enum pour le status
public enum UserStatus
{
    Connecte,
    Deconnecte
}